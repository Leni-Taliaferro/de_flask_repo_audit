# de_flask_repo_audit
Drop-in Python file to check basic things and output text file logs
## how to use
drop copy of file in main directory, and run file locally. Outputs a series of text logs to that repo directly
TODO: Add gitignore to exclude those text files and the main one. 
